<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- A browser needs to load both HTTP and HTTPS sites -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
